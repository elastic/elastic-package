metricsets: ["query"]
data_stream:
  dataset: {{data_stream.dataset}}
period: {{period}}
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
driver: {{driver}}
sql_query: {{sql_query}}
sql_response_format: {{sql_response_format}}
