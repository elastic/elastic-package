version: '2.3'
services:
  mongodb:
    build:
      context: .
      args:
        - SERVICE_VERSION=${SERVICE_VERSION:-7.0}
    ports:
      - 27017:27017
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.runCommand({serverStatus:1}).ok"]
      interval: 1s
      retries: 90