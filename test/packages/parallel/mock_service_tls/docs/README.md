# Mock Service TLS

Test package that exercises on-disk TLS certificate generation for mock service containers. The mock HTTP server runs with TLS using certificates generated by elastic-package before docker compose up.
