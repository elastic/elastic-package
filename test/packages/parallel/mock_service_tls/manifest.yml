format_version: 3.0.2
name: mock_service_tls
title: Mock Service TLS
description: Test package for on-disk TLS certificate generation in mock services.
type: integration
version: "999.999.999"
conditions:
  kibana:
    version: "^8.13.0"
categories:
  - custom
policy_templates:
  - name: mock_service_tls
    title: Mock Service TLS
    description: Collect data from a TLS-enabled mock API.
    inputs:
      - type: httpjson
        title: Collect data from mock API over HTTPS
        description: Collect data from mock API over HTTPS.
owner:
  github: elastic/security-service-integrations
  type: elastic
