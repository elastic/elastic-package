# CI composable input package

`type: input` package consumed by [`02_ci_composable_integration`](../02_ci_composable_integration/). Used in CI (`test-build-install-zip.sh`) and `internal/requiredinputs` tests. Build the input package before the integration when using a local registry.
