# Dashboards-as-code Sample

Fixture exercising the dashboards-as-code build step. The dashboard
source lives in `_dev/shared/` and is compiled into `kibana/dashboard/`
during `elastic-package build` against a running Kibana 9.4.0+ stack.
