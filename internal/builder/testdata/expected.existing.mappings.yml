title: existing
elasticsearch:
  index_template:
    mappings:
      properties:
        example:
          type: short
      dynamic_templates:
        - _embedded_ecs-example_template:
            mapping:
              type: short
            patch_match: example.*
