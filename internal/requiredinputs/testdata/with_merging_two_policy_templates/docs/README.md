# Variable Merging - Two Policy Templates

Test fixture: promotion of input-package vars is scoped to the policy
template's `data_streams` list. One template promotes `paths`; the other
leaves all vars on the data stream.
