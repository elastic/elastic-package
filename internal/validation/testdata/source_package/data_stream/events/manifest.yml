title: Events
type: logs
streams:
  - input: logfile
    title: Events logs
    description: Collect events log data
    vars:
      - name: paths
        type: text
        title: Paths
        multi: true
        required: true
        show_user: true
        default:
          - /var/log/*.log
