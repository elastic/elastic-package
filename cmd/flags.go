package cmd

// Fail immediately if any file requires updates
// Flag used by commands: check, format
const (
	failFastFlagName        = "fail-fast"
	failFastFlagDescription = "fail immediately if any file requires updates"
)
